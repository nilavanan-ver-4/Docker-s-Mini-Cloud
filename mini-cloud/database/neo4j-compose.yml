version: "3.9"
services:
  neo4j:
    image: neo4j:latest
    ports:
      - "7474:7474"  # Web UI
      - "7687:7687"  # Bolt protocol
    environment:
      NEO4J_AUTH: neo4j/test

volumes:
  neo4j_data:
