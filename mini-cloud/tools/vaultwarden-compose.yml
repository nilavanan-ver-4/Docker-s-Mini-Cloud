version: '3'
services:
  vaultwarden:
    image: vaultwarden/server
    container_name: vaultwarden
    ports:
      - "8085:80"
    volumes:
      - vaultwarden-data:/data
volumes:
  vaultwarden-data:
